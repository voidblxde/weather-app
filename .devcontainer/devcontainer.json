{
  "name": "Weather CLI Dev Environment",
  "image": "mcr.microsoft.com/devcontainers/java:1-21",
  "workspaceFolder": "/workspaces/weather-app",
  "settings": {
    "files.encoding": "utf8"
  },
  "customizations": {
    "vscode": {
      "extensions": [
        "vscjava.vscode-java-pack",
        "vscjava.vscode-maven",
        "redhat.java"
      ]
    }
  },
  "postCreateCommand": "apt-get update && apt-get install -y maven && echo '✅ Java + Maven готовы. Команда: cd cli && mvn compile exec:java \"-Dexec.mainClass=Main\"'"
}
